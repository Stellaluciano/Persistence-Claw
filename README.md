# Persistence-Claw
Persistence Claw is a neuroscience-inspired visual persistence layer for OpenClaw that dramatically reduces token usage in desktop agents by maintaining an iconic visual memory of the screen and updating only regions that change, instead of repeatedly re-analyzing full screenshots.
